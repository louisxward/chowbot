services:
  chowbot:
    container_name: chowbot
    restart: unless-stopped
    build: .
    expose:
      - "33002"
    env_file:
      - .env
    volumes:
      - /opt/data/chowbot:/app/data
      - /var/log/chowbot:/app/log
